name: feature-pipeline
on: [push] # Triggers the workflow on every push to the repository
jobs:
  # Job name
  feature-pipeline:
    uses: satishlakamsani/orders-actions/workflows/.github/workflows/nodejs-feature-pipeline.yaml@main
    with:
      project: 'roboshop'
      component: 'catalogue'
      environment: 'deve' 
  